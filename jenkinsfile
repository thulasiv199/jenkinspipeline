pipeline {
    agent any
    stages {
           stage('checkout'){
            steps{
           git branch: 'main', url: 'https://github.com/thulasiv199/bashscripts.git'
           }
           }
            stage('build'){
                steps{
                    sh "mvn clean install" 
                }
            }
    }
    }
    
           
